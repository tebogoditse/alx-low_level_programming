#include "main.h"

/**
 * _isupper - checks whether the alphabet is uppercase or not
 * @c: character to check
 * Return: True Or false (1 or 0);
 */

int	_isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
