#include "main.h"

/**
 * _abs - computes the interger's absolute value
 * @n: value to be computed
 * Return: The absolute value of an interger
 */

int	_abs(int n)
{
	if (n > -1)
		return (n);
	return (-n);
}
