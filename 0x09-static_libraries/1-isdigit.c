#include "main.h"

/**
 * _isdigit - checks whether the parameter is a number or not
 * @c: character to check
 * Return: True or False (1 or 0)
 */

int	_isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
