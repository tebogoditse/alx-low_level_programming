#include "main.h"

/**
 * _islower - checks whether the parameter is lowercase or not
 * @c: character to check
 * Return: True or False (1 or 0)
 */

int	_islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
